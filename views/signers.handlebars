<div class="signers-main">
    <div class="signers-parent">
        <h1>See everyone who is ready to change the flow of time!</h1>
        {{#arrayOfResults}}
            <li>
            {{#if homepage}}<a href="{{homepage}}">{{/if}}
                        {{first}} {{last}}
                {{#if homepage}}</a>{{/if}}

                {{#if age}} {{age}} {{/if}}

                {{#if city}}<a href="/signers/{{city}}">{{city}}</a>{{/if}}
            </li>
        {{/arrayOfResults}}
        </div>
    </div>
</div>